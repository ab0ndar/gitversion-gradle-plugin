pluginManagement {
    repositories {
        maven {
            url 'https://s01.oss.sonatype.org/content/groups/staging/'
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'gitversion-gradle-plugin'
include(':git-version')

